import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="w-full p-4 bg-white text-black">
      <h1 className="text-xl">Header</h1>
    </header>
  );
};

export default Header;
